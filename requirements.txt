# Smart Analysis -- runtime dependencies
#
# Project metadata and optional test/dev extras live in pyproject.toml.
# Install with::
#
#     pip install -r requirements.txt          # runtime only
#     pip install -e .[test]                   # runtime + test deps
#

pyyaml>=6.0
