# Two steps in two different conda envs, run sequentially.
# 'here' has no METADATA, so it runs in the orchestrator's env.
# 'elsewhere' declares METADATA["environment"], so it runs in that env.

metadata:
  functions_dir: "./steps"
  verbose: 0

env-isolation:
  - here:
  - elsewhere:
