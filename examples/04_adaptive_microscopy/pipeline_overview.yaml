# Cheap overview pass: every tile is recorded, then a scoped feedback
# step picks the bright ones from the whole region.

metadata:
  functions_dir: "./steps"
  verbose: 0

overview:
  - overview_tile:

  - feedback:
      scope: region
      threshold: 50
